source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Ruby 3 dropped webrick from the standard library; `jekyll serve` needs it.
gem "webrick", "~> 1.8"

# No plugins are used, which keeps the site buildable by either GitHub Pages
# build method. Add any you like to this group.
group :jekyll_plugins do
end
